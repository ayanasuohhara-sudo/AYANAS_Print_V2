"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.retry = exports.isRetryableKintoneError = void 0;
const promises_1 = require("timers/promises");
const rest_api_client_1 = require("@kintone/rest-api-client");
/**
 * Transient kintone REST API errors that should be retried even though they
 * are not 5xx. Extend this list as new retryable codes are found.
 *
 * - GAIA_DA02: returned with the message "Please wait a while and try again."
 */
const RETRYABLE_KINTONE_ERRORS = [{ status: 400, code: "GAIA_DA02" }];
/**
 * Returns true if the given error is a transient kintone REST API error
 * worth retrying: any 5xx, or an entry in RETRYABLE_KINTONE_ERRORS.
 */
const isRetryableKintoneError = (e) => {
    if (!(e instanceof rest_api_client_1.KintoneRestAPIError)) {
        return false;
    }
    if (e.status >= 500 && e.status < 600) {
        return true;
    }
    return RETRYABLE_KINTONE_ERRORS.some((entry) => entry.status === e.status && entry.code === e.code);
};
exports.isRetryableKintoneError = isRetryableKintoneError;
/**
 * Retry given function with exponential backoff with jitter strategy.
 * Delay time before retry increases exponentially until it reaches maxDelay.
 * @param fn A function to run.
 * @param options Options.
 * @param options.onError Callback function on rejection on each attempt.
 * @param options.retryCondition Condition function for whether to retry.
 * @param options.maxAttempt Max attempt count. Default to 5.
 * @param options.initialDelay Initial delay milliseconds before retry. Default to 1000ms.
 * @param options.maxDelay Maximum delay milliseconds before retry. Default to 60,000ms. The maxDelay value is inclusive.
 * @param options.maxJitter Maximum jitter milliseconds. Default to 1000ms. The maxJitter value is exclusive.
 */
const retry = async (fn, options) => {
    var _a, _b, _c, _d, _f, _g;
    const onError = (_a = options === null || options === void 0 ? void 0 : options.onError) !== null && _a !== void 0 ? _a : (() => {
        /* noop */
    });
    const retryCondition = (_b = options === null || options === void 0 ? void 0 : options.retryCondition) !== null && _b !== void 0 ? _b : defaultRetryCondition;
    const maxAttempt = (_c = options === null || options === void 0 ? void 0 : options.maxAttempt) !== null && _c !== void 0 ? _c : 5;
    const initialDelay = (_d = options === null || options === void 0 ? void 0 : options.initialDelay) !== null && _d !== void 0 ? _d : 1000;
    const maxDelay = (_f = options === null || options === void 0 ? void 0 : options.maxDelay) !== null && _f !== void 0 ? _f : 60 * 1000;
    const maxJitter = (_g = options === null || options === void 0 ? void 0 : options.maxJitter) !== null && _g !== void 0 ? _g : 1000;
    let finalError;
    for (let attemptCount = 1, delay = 0; attemptCount <= maxAttempt; attemptCount++) {
        await (0, promises_1.setTimeout)(delay);
        try {
            // To catch an error in this try-catch block, we need to await this function before return
            // ref. https://zenn.dev/azukiazusa/articles/difference-between-return-and-return-await
            return await fn();
        }
        catch (e) {
            finalError = e;
            // Plan next retry
            const retryCond = retryCondition(e);
            const jitter = Math.floor(Math.random() * maxJitter);
            delay =
                Math.min(initialDelay * 2 ** (attemptCount - 1), maxDelay) + jitter;
            await onError(e, attemptCount, retryCond, delay, {
                maxAttempt,
                initialDelay,
                maxDelay,
                maxJitter,
            });
            // Throw immediately if retry conditions are not met
            if (!retryCond) {
                throw e;
            }
        }
    }
    throw finalError;
};
exports.retry = retry;
const defaultRetryCondition = (_e) => true;
//# sourceMappingURL=retry.js.map