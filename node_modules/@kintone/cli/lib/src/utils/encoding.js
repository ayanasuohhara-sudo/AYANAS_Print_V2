"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.isMismatchEncoding = void 0;
const fs_1 = __importDefault(require("fs"));
const iconv_lite_1 = __importDefault(require("iconv-lite"));
const readline_1 = __importDefault(require("readline"));
const file_1 = require("./file");
const stream_1 = require("stream");
const isMismatchEncoding = async (filePath, encoding) => {
    const format = (0, file_1.extractFileFormat)(filePath);
    switch (format) {
        case "csv":
            return isMismatchEncodingOfCsvFile(filePath, encoding);
    }
    return false;
};
exports.isMismatchEncoding = isMismatchEncoding;
const isMismatchEncodingOfCsvFile = async (filePath, encoding) => {
    const decodedFirstLine = await getDecodedFirstLine(filePath, encoding);
    return containsUntranslatableChars(decodedFirstLine);
};
const getDecodedFirstLine = async (filePath, encoding) => {
    const stream = fs_1.default.createReadStream(filePath);
    const decodedStream = stream.pipe(stream_1.Transform.from(iconv_lite_1.default.decodeStream(encoding)));
    stream.on("error", (e) => {
        decodedStream.destroy(e);
    });
    const reader = readline_1.default.createInterface({
        input: decodedStream,
    });
    const { value: firstRow } = await reader[Symbol.asyncIterator]().next();
    reader.close();
    return firstRow;
};
const containsUntranslatableChars = (content) => {
    const untranslatableChars = ["�", "?"];
    return untranslatableChars.some((char) => content.includes(char));
};
//# sourceMappingURL=encoding.js.map