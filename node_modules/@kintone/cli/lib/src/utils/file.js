"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.isDirectory = exports.isFile = exports.extractFileFormat = exports.readFile = exports.openFsStreamWithEncode = void 0;
const fs_1 = __importDefault(require("fs"));
const path_1 = __importDefault(require("path"));
const iconv_lite_1 = __importDefault(require("iconv-lite"));
const stream_1 = require("stream");
const openFsStreamWithEncode = (filePath, encoding = "utf8") => {
    const stream = fs_1.default.createReadStream(filePath);
    const decodedStream = stream.pipe(stream_1.Transform.from(iconv_lite_1.default.decodeStream(encoding)));
    stream.on("error", (e) => {
        decodedStream.destroy(e);
    });
    return decodedStream;
};
exports.openFsStreamWithEncode = openFsStreamWithEncode;
const readFile = async (filePath, encoding = "utf8") => {
    const format = (0, exports.extractFileFormat)(filePath);
    if (format === "json" && encoding !== "utf8") {
        throw new Error("source file is JSON and JSON MUST be encoded with UTF-8");
    }
    return new Promise((resolve, reject) => {
        (async () => {
            const stream = fs_1.default.createReadStream(filePath);
            stream.on("error", reject);
            const readWriteStream = stream.pipe(iconv_lite_1.default.decodeStream(encoding));
            const content = await readStream(readWriteStream);
            resolve({ content, format });
        })();
    });
};
exports.readFile = readFile;
const readStream = async (stream) => {
    let content = "";
    for await (const chunk of stream) {
        content += chunk;
    }
    return content;
};
const extractFileFormat = (filepath) => {
    // TODO this cannot detect file format without extensions
    return path_1.default.extname(filepath).split(".").pop() || "";
};
exports.extractFileFormat = extractFileFormat;
/**
 * Check if the given path is a file.
 * Wrapping fs.stat() to reduce error handling code.
 * @param filePath
 */
const isFile = async (filePath) => {
    try {
        return (await fs_1.default.promises.stat(filePath)).isFile();
    }
    catch (_) {
        return false;
    }
};
exports.isFile = isFile;
/**
 * Check if the given path is a directory.
 * Wrapping fs.stat() to reduce error handling code.
 * @param dirPath
 */
const isDirectory = async (dirPath) => {
    try {
        return (await fs_1.default.promises.stat(dirPath)).isDirectory();
    }
    catch (_) {
        return false;
    }
};
exports.isDirectory = isDirectory;
//# sourceMappingURL=file.js.map