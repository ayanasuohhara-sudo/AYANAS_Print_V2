"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.iterToAsyncIter = iterToAsyncIter;
exports.chunked = chunked;
exports.groupByKey = groupByKey;
exports.groupByKeyChunked = groupByKeyChunked;
exports.withNext = withNext;
exports.withIndex = withIndex;
// eslint-disable-next-line func-style
async function* iterToAsyncIter(source) {
    for (const element of source) {
        yield element;
    }
}
// eslint-disable-next-line func-style
async function* chunked(source, size) {
    let chunk = [];
    for await (const element of source) {
        chunk.push(element);
        if (chunk.length >= size) {
            yield chunk;
            chunk = [];
        }
    }
    if (chunk.length > 0) {
        yield chunk;
    }
}
// eslint-disable-next-line func-style
async function* groupByKey(source, keyFn) {
    let array = [];
    for await (const { current, next } of withNext(source)) {
        array.push(current);
        const currentKey = keyFn(current);
        if (next === undefined || currentKey !== keyFn(next)) {
            yield { key: currentKey, data: array };
            array = [];
        }
    }
}
// eslint-disable-next-line func-style
async function* groupByKeyChunked(source, keyFn, size) {
    for await (const { key, data } of groupByKey(source, keyFn)) {
        for await (const chunk of chunked(iterToAsyncIter(data[Symbol.iterator]()), size)) {
            yield { key, data: chunk };
        }
    }
}
// eslint-disable-next-line func-style
async function* withNext(source) {
    let { value: prev, done } = await source.next();
    if (done) {
        return;
    }
    for await (const value of source) {
        yield { current: prev, next: value };
        prev = value;
    }
    yield { current: prev, next: undefined };
}
// eslint-disable-next-line func-style
async function* withIndex(source) {
    let index = 0;
    for await (const value of source) {
        yield { data: value, index };
        index++;
    }
}
//# sourceMappingURL=iterator.js.map