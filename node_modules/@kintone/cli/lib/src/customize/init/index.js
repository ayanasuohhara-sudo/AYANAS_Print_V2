"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.runInit = exports.generateCustomizeManifest = exports.getInitCustomizeManifest = void 0;
const promises_1 = __importDefault(require("fs/promises"));
const path_1 = __importDefault(require("path"));
const prompts_1 = require("@inquirer/prompts");
const log_1 = require("../../utils/log");
const core_1 = require("../core");
const file_1 = require("../../utils/file");
const getInitCustomizeManifest = () => {
    return {
        scope: "ALL",
        desktop: {
            js: [],
            css: [],
        },
        mobile: {
            js: [],
            css: [],
        },
    };
};
exports.getInitCustomizeManifest = getInitCustomizeManifest;
const generateCustomizeManifest = async (customizeManifest, outputPath) => {
    const destDir = path_1.default.dirname(outputPath);
    log_1.logger.debug(`Writing manifest to: ${outputPath}`);
    log_1.logger.debug(`Creating directory: ${destDir}`);
    await promises_1.default.mkdir(destDir, { recursive: true });
    const manifestJson = JSON.stringify(customizeManifest, null, 4);
    await promises_1.default.writeFile(outputPath, manifestJson);
    return manifestJson;
};
exports.generateCustomizeManifest = generateCustomizeManifest;
const runInit = async (params) => {
    const { outputPath, yes } = params;
    const m = (0, core_1.getBoundMessage)("en");
    log_1.logger.debug(`Output path: ${outputPath}`);
    // Check if file already exists and prompt for overwrite
    if ((await (0, file_1.isFile)(outputPath)) && !yes) {
        log_1.logger.debug(`File already exists: ${outputPath}`);
        const shouldOverwrite = await (0, prompts_1.confirm)({
            message: `File "${outputPath}" already exists. Overwrite?`,
            default: false,
        });
        if (!shouldOverwrite) {
            log_1.logger.info("Operation cancelled.");
            return;
        }
    }
    const customizeManifest = (0, exports.getInitCustomizeManifest)();
    await (0, exports.generateCustomizeManifest)(customizeManifest, outputPath);
    log_1.logger.info(`${outputPath} ${m("M_CommandInitFinish")}`);
};
exports.runInit = runInit;
//# sourceMappingURL=index.js.map