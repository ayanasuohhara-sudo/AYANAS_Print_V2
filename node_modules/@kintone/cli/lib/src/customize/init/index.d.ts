import type { CustomizeManifest } from "../core";
export interface InitParams {
    outputPath: string;
    yes: boolean;
}
export declare const getInitCustomizeManifest: () => CustomizeManifest;
export declare const generateCustomizeManifest: (customizeManifest: CustomizeManifest, outputPath: string) => Promise<string>;
export declare const runInit: (params: InitParams) => Promise<void>;
