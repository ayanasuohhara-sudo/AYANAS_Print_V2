"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.pack = void 0;
const path_1 = __importDefault(require("path"));
const promises_1 = __importDefault(require("fs/promises"));
const os_1 = __importDefault(require("os"));
const chokidar = __importStar(require("chokidar"));
const log_1 = require("../../utils/log");
const core_1 = require("../core");
const file_1 = require("../../utils/file");
// TODO: Reduce statements in this func
const pack = async (params) => {
    const manifestJsonFilePath = path_1.default.resolve(params.input);
    log_1.logger.debug(`input manifest file path: ${manifestJsonFilePath}`);
    const outputFilePath = path_1.default.resolve(params.output || "./plugin.zip");
    log_1.logger.debug(`output file path: ${outputFilePath}`);
    // Root directory of source files
    // Default to the directory where manifest.json is located
    const sourceRootDir = path_1.default.resolve(path_1.default.dirname(manifestJsonFilePath));
    // 1. Validate input parameters
    // Check manifest.json exists
    if (!(await (0, file_1.isFile)(manifestJsonFilePath))) {
        throw new Error(`Manifest file not found: ${manifestJsonFilePath}`);
    }
    // Check ppk exists
    const ppkFilePath = path_1.default.resolve(params.ppkFilePath);
    if (!(await (0, file_1.isFile)(ppkFilePath))) {
        throw new Error(`Private key file not found: ${ppkFilePath}`);
    }
    // 2. Load manifest.json
    const manifest = await core_1.ManifestFactory.loadJsonFile(manifestJsonFilePath);
    if (manifest.manifestVersion === 2) {
        log_1.logger.warn("Welcome to manifest v2 mode :)");
    }
    // 3. Validate manifest.json
    const result = await manifest.validate(new core_1.LocalFSDriver(sourceRootDir));
    if (result.warnings.length > 0) {
        result.warnings.forEach((warning) => {
            log_1.logger.warn(warning);
        });
    }
    if (!result.valid) {
        log_1.logger.error("Invalid manifest.json:");
        result.errors.forEach((msg) => {
            log_1.logger.error(`- ${msg}`);
        });
        throw new Error("Invalid manifest.json");
    }
    // 4. Prepare output directory
    await promises_1.default.mkdir(path_1.default.dirname(outputFilePath), { recursive: true });
    log_1.logger.debug(`outputFile: ${outputFilePath}`);
    // 5. Load ppk or generate new ppk if not specified
    log_1.logger.debug(`loading an existing key: ${ppkFilePath}`);
    const ppk = await promises_1.default.readFile(ppkFilePath, "utf8");
    const privateKey = core_1.PrivateKey.importKey(ppk);
    const id = privateKey.uuid();
    log_1.logger.debug(`plugin id: ${id}`);
    // 6. Package plugin.zip
    const pluginZip = await core_1.PluginZip.build(manifest, privateKey, new core_1.LocalFSDriver(sourceRootDir));
    // 7. Start watch mode if watch option is given
    if (params.watch) {
        // change events are fired before changed files are flushed on Windows,
        // which generate an invalid plugin zip.
        // in order to fix this, we use awaitWriteFinish option only on Windows.
        const watchOptions = os_1.default.platform() === "win32"
            ? {
                awaitWriteFinish: {
                    stabilityThreshold: 1000,
                    pollInterval: 250,
                },
            }
            : {};
        const watchTargets = [
            manifestJsonFilePath,
            ...manifest.sourceList().map((src) => path_1.default.resolve(sourceRootDir, src)),
        ];
        const watcher = chokidar.watch(watchTargets, watchOptions);
        watcher.on("change", () => {
            (0, exports.pack)({
                ...params,
                watch: false,
            });
        });
    }
    // 8. Write out plugin.zip to filesystem
    await promises_1.default.writeFile(outputFilePath, pluginZip.buffer);
    log_1.logger.info(`The plugin file generated: ${outputFilePath}`);
};
exports.pack = pack;
//# sourceMappingURL=pack.js.map