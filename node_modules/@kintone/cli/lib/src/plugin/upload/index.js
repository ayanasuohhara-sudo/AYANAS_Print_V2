"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.upload = void 0;
const promises_1 = __importDefault(require("fs/promises"));
const prompts_1 = require("@inquirer/prompts");
const core_1 = require("../core");
const summary_1 = require("../core/summary");
const client_1 = require("../../kintone/client");
const log_1 = require("../../utils/log");
const os_1 = __importDefault(require("os"));
const chokidar = __importStar(require("chokidar"));
const path_1 = __importDefault(require("path"));
const file_1 = require("../../utils/file");
const upload = async (params) => {
    var _a;
    const { pluginFilePath: _, ...restApiClientOptions } = params;
    const pluginFilePath = path_1.default.resolve(params.pluginFilePath);
    if (!(await (0, file_1.isFile)(pluginFilePath))) {
        throw new Error(`Plugin file not found: ${pluginFilePath}`);
    }
    const apiClient = (0, client_1.buildRestAPIClient)(restApiClientOptions);
    // Read plugin zip file
    const buffer = await promises_1.default.readFile(pluginFilePath);
    const pluginZip = await core_1.PluginZip.fromBuffer(buffer);
    const pluginId = await pluginZip.getPluginID();
    const pluginManifest = await pluginZip.manifest();
    // Read plugin info from kintone
    const { plugins: installedPlugins } = await apiClient.plugin.getPlugins(
    // TODO: rest-api-client should accept array of ids
    {
        ids: [pluginId],
    });
    const installedPlugin = installedPlugins.find((p) => p.id === pluginId);
    const isInstalled = installedPlugin !== undefined;
    const isSameVersion = (installedPlugin === null || installedPlugin === void 0 ? void 0 : installedPlugin.version) === pluginManifest.version;
    // Show installation summary
    const summary = (0, summary_1.buildPluginSummary)(pluginId, pluginManifest);
    const lines = [
        `    Destination: ${restApiClientOptions.baseUrl}`,
        `    File Path: ${pluginFilePath}`,
        `    Plugin ID: ${summary.id}`,
        `    Plugin Name: ${summary.name}`,
        `    Current version: ${(_a = installedPlugin === null || installedPlugin === void 0 ? void 0 : installedPlugin.version) !== null && _a !== void 0 ? _a : "(not installed)"}`,
        `    Target version: ${summary.version}${isSameVersion ? " (reinstall)" : ""}`,
        ...(summary.sandbox !== null
            ? [
                `    Sandbox: ${summary.sandbox.sandbox}`,
                `    Allowed hosts: ${summary.sandbox.allowedHosts}`,
                `    Permissions: ${summary.sandbox.permissions}`,
            ]
            : []),
    ];
    const installationSummary = `\n  Installation Summary:\n${lines.join("\n")}`;
    log_1.logger.info(installationSummary);
    // Get confirmation from user if required
    if (!params.force && !params.watch) {
        const answers = await (0, prompts_1.confirm)({
            message: `Do you want to continue?`,
            default: false,
        });
        if (!answers) {
            log_1.logger.info("Plugin installation is cancelled by the user.");
            return;
        }
    }
    // Upload plugin zip file to kintone
    const { fileKey } = await apiClient.file.uploadFile({
        file: { name: pluginFilePath, data: buffer },
    });
    // Install or update plugin
    if (isInstalled) {
        await apiClient.plugin.updatePlugin({ id: pluginId, fileKey });
    }
    else {
        await apiClient.plugin.installPlugin({ fileKey });
    }
    // Start watch mode if watch option is given
    if (params.watch) {
        // TODO: 他のコマンドとwatchモードに渡すオプションを共通化する
        // change events are fired before changed files are flushed on Windows,
        // which generate an invalid plugin zip.
        // in order to fix this, we use awaitWriteFinish option only on Windows.
        const watchOptions = os_1.default.platform() === "win32"
            ? {
                awaitWriteFinish: {
                    stabilityThreshold: 1000,
                    pollInterval: 250,
                },
            }
            : {};
        const watcher = chokidar.watch(pluginFilePath, watchOptions);
        watcher.on("change", async () => {
            await (0, exports.upload)({ ...params, force: true, watch: false });
        });
    }
    log_1.logger.info("Plugin is installed successfully.");
};
exports.upload = upload;
//# sourceMappingURL=index.js.map