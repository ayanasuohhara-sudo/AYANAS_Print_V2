import { type RestAPIClientOptions } from "../../kintone/client";
export type Params = {
    pluginFilePath: string;
    force?: boolean;
    watch?: boolean;
};
export declare const upload: (params: Params & RestAPIClientOptions) => Promise<void>;
