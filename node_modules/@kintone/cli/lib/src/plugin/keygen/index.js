"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.keygen = void 0;
const promises_1 = __importDefault(require("fs/promises"));
const core_1 = require("../core");
const path_1 = __importDefault(require("path"));
const log_1 = require("../../utils/log");
const file_1 = require("../../utils/file");
const keygen = async (output) => {
    // Generate new ppk
    const privateKey = core_1.PrivateKey.generateKey();
    const id = privateKey.uuid();
    log_1.logger.debug(`plugin id: ${id}`);
    // Write out ppk to filesystem
    const outputFilePath = path_1.default.resolve(output ? output : `./${id}.ppk`);
    if (await (0, file_1.isFile)(outputFilePath)) {
        throw new Error(`File ${outputFilePath} already exists.`);
    }
    const outputDir = path_1.default.dirname(outputFilePath);
    await promises_1.default.mkdir(outputDir, { recursive: true });
    log_1.logger.debug(`output: ${outputFilePath}`);
    await promises_1.default.writeFile(outputFilePath, privateKey.exportPrivateKey(), "utf8");
    log_1.logger.info(`New private key generated: ${outputFilePath}`);
};
exports.keygen = keygen;
//# sourceMappingURL=index.js.map