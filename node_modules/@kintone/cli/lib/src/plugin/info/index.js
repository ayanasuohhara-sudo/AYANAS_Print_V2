"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.run = exports.buildPluginInfoJson = void 0;
const promises_1 = __importDefault(require("fs/promises"));
const core_1 = require("../core");
const summary_1 = require("../core/summary");
const buildPluginInfoJson = (id, manifest) => ({
    id,
    name: manifest.name,
    version: manifest.version,
    description: manifest.description,
    homepage: manifest.homepageUrl,
    sandbox: manifest.sandbox,
    allowed_hosts: manifest.allowedHosts,
    permissions: manifest.permissions,
});
exports.buildPluginInfoJson = buildPluginInfoJson;
const run = async (pluginFilePath, format) => {
    const buffer = await promises_1.default.readFile(pluginFilePath);
    const pluginZip = await core_1.PluginZip.fromBuffer(buffer);
    const id = await pluginZip.getPluginID();
    const manifest = await pluginZip.manifest();
    switch (format) {
        case "plain": {
            const summary = (0, summary_1.buildPluginSummary)(id, manifest);
            console.log("id:", summary.id);
            console.log("name:", summary.name);
            console.log("version:", summary.version);
            console.log("description:", summary.description);
            console.log("homepage:", summary.homepage);
            if (summary.sandbox !== null) {
                const s = summary.sandbox;
                console.log("sandbox:", s.sandbox);
                console.log("allowed_hosts:", s.allowedHosts);
                console.log("permissions:", s.permissions);
            }
            break;
        }
        case "json": {
            console.log(JSON.stringify((0, exports.buildPluginInfoJson)(id, manifest), null, 2));
            break;
        }
    }
};
exports.run = run;
//# sourceMappingURL=index.js.map