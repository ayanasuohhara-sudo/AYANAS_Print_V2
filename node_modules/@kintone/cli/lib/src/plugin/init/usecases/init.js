"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.initPlugin = void 0;
const chalk_1 = __importDefault(require("chalk"));
const messages_1 = require("../utils/messages");
const template_1 = require("../utils/template");
const qa_1 = require("../utils/qa");
const log_1 = require("../../../utils/log");
const path_1 = __importDefault(require("path"));
const fs_1 = __importDefault(require("fs"));
const deps_1 = require("../utils/deps");
const keygen_1 = require("../utils/keygen");
const file_1 = require("../../../utils/file");
const getSuccessCreatedPluginMessage = (packageName, outputDir, lang) => {
    const m = (0, messages_1.getBoundMessage)(lang);
    return `

Success! Created ${packageName} at ${outputDir}

${chalk_1.default.cyan("npm start")}

  ${m("npmStart")}

${chalk_1.default.cyan("npm run build")}

  ${m("npmBuild")}

${chalk_1.default.cyan("npm run lint")}

  ${m("npmLint")}

${m("nextAction")}

  cd ${outputDir}
  npm start

${m("lastMessage")}
${m("developerSite")}

      `;
};
/**
 * Run create-kintone-plugin script
 */
const initPlugin = async (options) => {
    const m = (0, messages_1.getBoundMessage)(options.lang);
    log_1.logger.info(`

  ${m("introduction")}

  `);
    const answers = await (0, qa_1.runPrompt)(m, options.name);
    const packageName = path_1.default.basename(answers.projectName);
    log_1.logger.info(m("settingUpTemplate"));
    log_1.logger.debug(`template: ${options.template}`);
    // Verify output directory does not exist
    const directoryExists = await (0, file_1.isDirectory)(answers.projectName);
    if (directoryExists) {
        log_1.logger.error(`${answers.projectName} ${(0, messages_1.getMessage)(options.lang, "Error_alreadyExists")}`);
        // eslint-disable-next-line n/no-process-exit
        process.exit(1);
    }
    try {
        await (0, template_1.setupTemplate)({
            outputDir: answers.projectName,
            templateName: options.template,
            manifestPatch: {
                name: answers.name,
                description: answers.description,
                homepage_url: answers.homepage_url,
            },
            packageJsonPatch: {
                name: packageName,
            },
        });
        log_1.logger.info(m("templateSetupCompleted"));
        (0, deps_1.installDependencies)(answers.projectName, options.lang);
        (0, keygen_1.runKeygen)(answers.projectName, options.lang);
        log_1.logger.info(getSuccessCreatedPluginMessage(packageName, answers.projectName, options.lang));
    }
    catch (error) {
        try {
            await fs_1.default.promises.rm(answers.projectName, {
                recursive: true,
                force: true,
            });
            if (error instanceof Error) {
                log_1.logger.error(m("Error_cannotCreatePlugin") + error.message);
            }
            else {
                log_1.logger.error(m("Error_cannotCreatePlugin") + String(error));
            }
        }
        finally {
            // eslint-disable-next-line n/no-process-exit
            process.exit(1);
        }
    }
};
exports.initPlugin = initPlugin;
//# sourceMappingURL=init.js.map