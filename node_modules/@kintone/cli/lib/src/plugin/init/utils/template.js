"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.setupTemplate = void 0;
const path_1 = __importDefault(require("path"));
const github_1 = require("./template/github");
const downloader_1 = require("./template/downloader");
const manifest_1 = require("./template/manifest");
const pacakge_json_1 = require("./template/pacakge-json");
const log_1 = require("../../../utils/log");
const promises_1 = require("fs/promises");
const setupTemplate = async (opts) => {
    // Create output directory
    await (0, promises_1.mkdir)(opts.outputDir, { recursive: true });
    log_1.logger.debug(`downloading template: ${opts.templateName}`);
    // Resolve template source from GitHub (includes existence check)
    const source = await (0, github_1.resolveGitHubTemplateSource)(opts.templateName);
    // Download and extract template
    await (0, downloader_1.downloadAndExtractFromUrl)({
        source,
        outputDir: opts.outputDir,
    });
    log_1.logger.debug(`template downloaded: ${opts.templateName}`);
    await (0, manifest_1.updateManifests)({
        manifestPath: path_1.default.join(opts.outputDir, "manifest.json"),
        patch: opts.manifestPatch,
    });
    await (0, pacakge_json_1.updatePackageJson)({
        packageJsonPath: path_1.default.join(opts.outputDir, "package.json"),
        patch: opts.packageJsonPatch,
    });
};
exports.setupTemplate = setupTemplate;
//# sourceMappingURL=template.js.map