import type { Lang } from "./lang";
/**
 * Run npm run keygen to generate private key
 * @param targetDirectory
 * @param lang
 */
export declare const runKeygen: (targetDirectory: string, lang: Lang) => void;
