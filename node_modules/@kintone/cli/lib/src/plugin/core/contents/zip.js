"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.buildContentsZip = void 0;
const yazl_1 = __importDefault(require("yazl"));
const path_1 = __importDefault(require("path"));
const consumers_1 = __importDefault(require("node:stream/consumers"));
const log_1 = require("../../../utils/log");
/**
 * Build a zipped contents
 */
const buildContentsZip = async (manifest, driver) => {
    let size = NaN;
    const zipFile = new yazl_1.default.ZipFile();
    for (const src of manifest.sourceList()) {
        const data = await driver.readFile(src);
        const entryName = src === manifest.manifestFileName ? "manifest.json" : src;
        zipFile.addBuffer(data, entryName);
    }
    zipFile.end(undefined, ((finalSize) => {
        size = finalSize;
    }));
    log_1.logger.trace(`plugin.zip: ${size} bytes`);
    return consumers_1.default.buffer(zipFile.outputStream);
};
exports.buildContentsZip = buildContentsZip;
/**
 * Build a zipped contents using stream. Can be faster than buildContentsZip above.
 */
const _buildContentsZipStream = async (manifest, driver) => {
    // TODO: Support prefix
    //  The zip file not created plugin-packer can have additional parent directories
    const manifestPath = "";
    const manifestPrefix = path_1.default.dirname(manifestPath);
    let size = NaN;
    const zipFile = new yazl_1.default.ZipFile();
    const promises = manifest.sourceList().map(async (src) => {
        const entryName = path_1.default.join(manifestPrefix, src);
        const entry = await driver.stat(entryName);
        if (entry === undefined) {
            throw new Error(`Failed to find entry: ${entryName}`);
        }
        const readable = await driver.openReadStream(entryName);
        zipFile.addReadStream(readable, src, {
            size: entry.size,
        });
    });
    await Promise.all(promises);
    zipFile.end(undefined, ((finalSize) => {
        size = finalSize;
    }));
    log_1.logger.trace(`plugin.zip: ${size} bytes`);
    return consumers_1.default.buffer(zipFile.outputStream);
};
//# sourceMappingURL=zip.js.map