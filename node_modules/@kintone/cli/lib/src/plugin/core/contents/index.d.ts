import type { ManifestInterface } from "../manifest";
import type { DriverInterface } from "../driver";
import { ZipFileDriver } from "../driver";
/**
 * Contents represents the plugin source files aggregated by Manifest
 */
export interface ContentsInterface extends DriverInterface {
    /**
     * returns Manifest
     */
    manifest(): Promise<ManifestInterface>;
    /**
     * validate contents
     */
    validate(): Promise<void>;
}
export declare class ContentsZip extends ZipFileDriver implements ContentsInterface {
    /**
     * Use static factory methods instead.
     * @private
     */
    private constructor();
    static buildFromManifest(manifest: ManifestInterface, driver: DriverInterface): Promise<ContentsZip>;
    static fromBuffer(buffer: Buffer): Promise<ContentsZip>;
    manifest(): Promise<ManifestInterface>;
    validate(): Promise<void>;
}
