"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.PrivateKey = void 0;
const crypto_1 = __importDefault(require("crypto"));
const uuid_1 = require("./uuid");
const sign_1 = require("./sign");
class PrivateKey {
    /**
     * Use static method generateKey() or importKey() instead.
     * @private
     */
    constructor(key) {
        this.key = key;
    }
    /**
     * Generate new private key
     */
    static generateKey() {
        const { privateKey } = crypto_1.default.generateKeyPairSync("rsa", {
            modulusLength: 1024,
        });
        return new PrivateKey(privateKey);
    }
    /**
     * Import private key
     * @param key {string} utf8 encoded ppk file
     */
    static importKey(key) {
        const privateKey = crypto_1.default.createPrivateKey({
            key,
            format: "pem",
        });
        return new PrivateKey(privateKey);
    }
    exportPrivateKey() {
        const pem = this.key.export({
            type: "pkcs1",
            format: "pem",
        });
        // Ensure trailing newline for compatibility
        return pem.endsWith("\n") ? pem : pem + "\n";
    }
    exportPublicKey() {
        const publicKey = crypto_1.default.createPublicKey(this.key);
        return publicKey.export({
            type: "spki",
            format: "der",
        });
    }
    uuid() {
        return (0, uuid_1.uuid)(this.exportPublicKey());
    }
    sign(contents) {
        return (0, sign_1.sign)(contents, this.key);
    }
}
exports.PrivateKey = PrivateKey;
//# sourceMappingURL=private-key.js.map