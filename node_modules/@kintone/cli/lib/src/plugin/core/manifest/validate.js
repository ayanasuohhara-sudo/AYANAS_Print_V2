"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.validateManifest = void 0;
const plugin_manifest_validator_1 = __importDefault(require("@kintone/plugin-manifest-validator"));
const validateManifest = async (manifest, driver) => {
    var _a, _b, _c;
    const options = driver
        ? {
            relativePath: validateRelativePath(driver),
            maxFileSize: validateMaxFileSize(driver),
            fileExists: validateFileExists(driver),
        }
        : {};
    const result = (0, plugin_manifest_validator_1.default)(manifest.json, options);
    const warnings = (_b = (_a = result.warnings) === null || _a === void 0 ? void 0 : _a.map((warn) => warn.message)) !== null && _b !== void 0 ? _b : [];
    if (result.valid) {
        return {
            valid: true,
            warnings,
        };
    }
    const errors = buildErrorMessages((_c = result.errors) !== null && _c !== void 0 ? _c : []);
    return {
        valid: false,
        warnings,
        errors,
    };
};
exports.validateManifest = validateManifest;
const buildErrorMessages = (errors) => {
    return errors.map((e) => {
        if (e.keyword === "enum") {
            return `"${e.instancePath}" ${e.message} (${e.params.allowedValues
                .map((v) => `"${v}"`)
                .join(", ")})`;
        }
        return `"${e.instancePath}" ${e.message}`;
    });
};
// TODO: Make below functions async after https://github.com/kintone/js-sdk/pull/3037 merged
const validateRelativePath = (driver) => (filePath) => {
    try {
        const stat = driver.statSync(filePath);
        return stat.isFile;
    }
    catch (_) {
        return false;
    }
};
/**
 * Return validator for `maxFileSize` keyword
 */
const validateMaxFileSize = (driver) => (maxBytes, filePath) => {
    try {
        const stat = driver.statSync(filePath);
        return stat.size <= maxBytes;
    }
    catch (e) {
        return { valid: false, message: e ? `${e}` : undefined };
    }
};
const validateFileExists = (driver) => (filePath) => {
    try {
        const stat = driver.statSync(filePath);
        return stat.isFile;
    }
    catch (e) {
        return { valid: false, message: e ? `${e}` : undefined };
    }
};
//# sourceMappingURL=validate.js.map