"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ManifestFactory = void 0;
const path_1 = __importDefault(require("path"));
const driver_1 = require("../driver");
const v1_1 = require("./v1");
const v2_1 = require("./v2");
/**
 * Factory to instantiate Manifest
 */
class ManifestFactory {
    constructor() {
        /* noop */
    }
    static parseJson(manifestJson) {
        const json = JSON.parse(manifestJson);
        // TODO: Replace this validation with Ajv
        if (!(typeof json === "object" && json !== null)) {
            throw new Error("The root of manifest must be object");
        }
        if (!("manifest_version" in json)) {
            throw new Error("manifest_version must be set in manifest.json");
        }
        if (typeof json.manifest_version !== "number") {
            throw new Error("manifest_version must be number");
        }
        switch (json.manifest_version) {
            case 1:
                return new v1_1.ManifestV1(json);
            case 2:
                return new v2_1.ManifestV2(json);
            default:
                throw new Error("manifest_version must be 1 or 2");
        }
    }
    static async loadJsonFile(jsonFilePath, driver) {
        const _driver = driver !== null && driver !== void 0 ? driver : new driver_1.LocalFSDriver();
        const manifestJson = await _driver.readFile(jsonFilePath, "utf-8");
        const manifest = this.parseJson(manifestJson);
        manifest.manifestFileName = path_1.default.basename(jsonFilePath);
        return manifest;
    }
}
exports.ManifestFactory = ManifestFactory;
const _ = ManifestFactory;
//# sourceMappingURL=factory.js.map