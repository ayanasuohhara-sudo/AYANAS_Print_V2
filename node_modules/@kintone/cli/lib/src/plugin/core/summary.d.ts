import type { ManifestInterface } from "./manifest/interface";
export type SandboxSummary = {
    sandbox: string;
    allowedHosts: string;
    permissions: string;
};
export type PluginSummary = {
    id: string;
    name: string;
    version: string;
    description: string;
    homepage: string;
    sandbox: SandboxSummary | null;
};
export declare const buildPluginSummary: (id: string, manifest: ManifestInterface) => PluginSummary;
