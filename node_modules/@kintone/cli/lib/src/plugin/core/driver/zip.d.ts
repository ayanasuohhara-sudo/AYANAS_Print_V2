import type { DriverInterface, Encoding, FileStats } from "./interface";
import type { Readable } from "node:stream";
/**
 * ZipFileDriver is a Driver for a zip file
 */
export declare class ZipFileDriver implements DriverInterface {
    private readonly _buffer;
    constructor(buffer: Buffer);
    fileList(): Promise<string[]>;
    stat(fileName: string): Promise<FileStats>;
    private _entriesCache?;
    cacheEntries(): Promise<void>;
    statSync(fileName: string): FileStats;
    readFile(fileName: string): Promise<Buffer>;
    readFile(fileName: string, encoding: Encoding): Promise<string>;
    openReadStream(fileName: string): Promise<Readable>;
    writeFile(_fileName: string, _data: string | Buffer): Promise<void>;
    get buffer(): Buffer<ArrayBufferLike>;
    private unzip;
    private entry;
    private entries;
}
