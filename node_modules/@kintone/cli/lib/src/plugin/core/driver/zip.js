"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ZipFileDriver = void 0;
const yauzl_1 = __importDefault(require("yauzl"));
const util_1 = require("util");
const consumers_1 = __importDefault(require("node:stream/consumers"));
/**
 * ZipFileDriver is a Driver for a zip file
 */
class ZipFileDriver {
    constructor(buffer) {
        this._buffer = buffer;
    }
    async fileList() {
        const entries = await this.entries();
        return Array.from(entries.keys());
    }
    async stat(fileName) {
        const entry = await this.entry(fileName);
        return { size: entry.uncompressedSize, isFile: true };
    }
    // TODO: Delete this function after https://github.com/kintone/js-sdk/pull/3037 merged
    //   This is a workaround to implement synchronous stat operation
    async cacheEntries() {
        this._entriesCache = await this.entries();
    }
    statSync(fileName) {
        if (this._entriesCache === undefined) {
            throw new Error("Run cacheEntries() before call statSync");
        }
        const entry = this._entriesCache.get(fileName);
        if (entry === undefined) {
            throw new Error(`Failed to find entry from the zip file: ${entry}`);
        }
        return { size: entry.uncompressedSize, isFile: true };
    }
    async readFile(fileName, encoding) {
        const entry = await this.entry(fileName);
        const zipFile = await this.unzip();
        const stream = await (0, util_1.promisify)(zipFile.openReadStream).bind(zipFile)(entry);
        const contents = await consumers_1.default.buffer(stream);
        switch (encoding) {
            case "utf-8":
                return contents.toString("utf-8");
            case null:
            case undefined:
            default:
                return contents;
        }
    }
    async openReadStream(fileName) {
        const entry = await this.entry(fileName);
        const zipFile = await this.unzip();
        return (0, util_1.promisify)(zipFile.openReadStream).bind(zipFile)(entry);
    }
    async writeFile(_fileName, _data) {
        // TODO: Implement
        throw new Error("This function is not implemented yet");
    }
    get buffer() {
        return this._buffer;
    }
    async unzip() {
        return (0, util_1.promisify)(yauzl_1.default.fromBuffer)(this._buffer);
    }
    async entry(fileName) {
        const entries = await this.entries();
        const entry = entries.get(fileName);
        if (entry === undefined) {
            throw new Error(`Failed to find entry from the zip file: ${entry}`);
        }
        return entry;
    }
    async entries() {
        const zipFile = await this.unzip();
        return new Promise((res, rej) => {
            const entries = new Map();
            zipFile === null || zipFile === void 0 ? void 0 : zipFile.on("entry", (entry) => {
                entries.set(entry.fileName, entry);
            });
            zipFile === null || zipFile === void 0 ? void 0 : zipFile.on("end", () => {
                res(entries);
            });
            zipFile === null || zipFile === void 0 ? void 0 : zipFile.on("error", rej);
        });
    }
}
exports.ZipFileDriver = ZipFileDriver;
//# sourceMappingURL=zip.js.map