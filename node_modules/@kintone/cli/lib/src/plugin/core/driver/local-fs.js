"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.LocalFSDriver = void 0;
const promises_1 = __importDefault(require("fs/promises"));
const fs_1 = require("fs");
const path_1 = __importDefault(require("path"));
/**
 * LocalFSDriver is a Driver for the local disc
 */
class LocalFSDriver {
    constructor(dir) {
        this.currentDir = dir !== null && dir !== void 0 ? dir : process.cwd();
    }
    async fileList() {
        const files = await promises_1.default.readdir(this.currentDir, { withFileTypes: true });
        return files.filter((file) => file.isFile()).map((f) => f.name);
    }
    async stat(fileName) {
        const filePath = path_1.default.resolve(this.currentDir, fileName);
        const stats = await promises_1.default.stat(filePath);
        stats.isFile();
        return {
            isFile: stats.isFile(),
            size: stats.size,
        };
    }
    statSync(fileName) {
        const filePath = path_1.default.resolve(this.currentDir, fileName);
        const stats = (0, fs_1.statSync)(filePath);
        stats.isFile();
        return {
            isFile: stats.isFile(),
            size: stats.size,
        };
    }
    async readFile(fileName, encoding) {
        const filePath = path_1.default.resolve(this.currentDir, fileName);
        return promises_1.default.readFile(filePath, {
            encoding: encoding,
        });
    }
    async openReadStream(fileName) {
        const filePath = path_1.default.resolve(this.currentDir, fileName);
        return (0, fs_1.createReadStream)(filePath);
    }
    async writeFile(fileName, data) {
        const filePath = path_1.default.resolve(this.currentDir, fileName);
        return promises_1.default.writeFile(filePath, data);
    }
}
exports.LocalFSDriver = LocalFSDriver;
//# sourceMappingURL=local-fs.js.map