import type { PrivateKeyInterface } from "../crypto";
import { ContentsZip } from "../contents";
import type { DriverInterface } from "../driver";
import { ZipFileDriver } from "../driver";
import type { ManifestInterface } from "../manifest";
/**
 * Plugin represents plugin includes Contents, public key, and signature.
 */
export interface PluginInterface extends DriverInterface {
}
export declare class PluginZip extends ZipFileDriver implements PluginInterface {
    /**
     * Use static factory methods instead.
     * @private
     */
    private constructor();
    static fromBuffer(buffer: Buffer): Promise<PluginZip>;
    /**
     * Build plugin.zip
     */
    static build(manifest: ManifestInterface, privateKey: PrivateKeyInterface, driver: DriverInterface): Promise<PluginZip>;
    /**
     * Build plugin.zip from contents.zip
     */
    static buildFromContentsZip(contentsZip: ContentsZip, privateKey: PrivateKeyInterface): Promise<PluginZip>;
    manifest(): Promise<ManifestInterface>;
    private contentsZip;
    getPluginID(): Promise<string>;
}
