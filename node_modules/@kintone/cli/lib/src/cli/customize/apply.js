"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.applyCommand = void 0;
const log_1 = require("../../utils/log");
const error_1 = require("../../record/error");
const connectionOptions_1 = require("../connectionOptions");
const apply_1 = require("../../customize/apply");
const command = "apply";
const describe = "Apply JavaScript/CSS customization to a kintone app";
const builder = (args) => (0, connectionOptions_1.buildConnectionOptions)(args, { auth: ["password"], guestSpace: true })
    .option("input", {
    alias: "i",
    describe: "The path to customize-manifest.json",
    type: "string",
    demandOption: true,
    requiresArg: true,
})
    .option("app", {
    describe: "The kintone app ID",
    type: "string",
    demandOption: true,
    requiresArg: true,
})
    .option("yes", {
    alias: "y",
    describe: "Skip confirmation prompts",
    type: "boolean",
    default: false,
})
    .option("watch", {
    describe: "Watch for file changes and re-apply",
    type: "boolean",
    default: false,
});
const handler = async (args) => {
    try {
        await (0, apply_1.runApply)({
            appId: args.app,
            inputPath: args.input,
            yes: args.yes,
            watch: args.watch,
            baseUrl: args["base-url"],
            username: args.username,
            password: args.password,
            basicAuthUsername: args["basic-auth-username"],
            basicAuthPassword: args["basic-auth-password"],
            guestSpaceId: args["guest-space-id"],
            pfxFilePath: args["pfx-file-path"],
            pfxFilePassword: args["pfx-file-password"],
            httpsProxy: args.proxy,
        });
    }
    catch (error) {
        log_1.logger.error(new error_1.RunError(error));
        // eslint-disable-next-line n/no-process-exit
        process.exit(1);
    }
};
exports.applyCommand = {
    command,
    describe,
    builder,
    handler,
};
//# sourceMappingURL=apply.js.map