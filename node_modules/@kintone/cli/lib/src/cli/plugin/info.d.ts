import type yargs from "yargs";
import type { CommandModule } from "yargs";
import type { OutputFormat } from "../../plugin/info/";
declare const builder: (args: yargs.Argv) => yargs.Argv<{
    input: string;
} & {
    format: OutputFormat;
}>;
type Args = yargs.Arguments<ReturnType<typeof builder> extends yargs.Argv<infer U> ? U : never>;
export declare const infoCommand: CommandModule<{}, Args>;
export {};
