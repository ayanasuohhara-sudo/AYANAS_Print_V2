"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.initCommand = void 0;
const log_1 = require("../../utils/log");
const error_1 = require("../../record/error");
const init_1 = require("../../plugin/init");
const command = "init";
const describe = "Initialize plugin project";
const builder = (args) => args
    .option("name", {
    describe: "The name of your plugin (and the target directory). If omitted, you will be prompted to input interactively.",
    type: "string",
    requiresArg: true,
})
    .option("template", {
    describe: "A template for a generated plug-in. See https://github.com/kintone/cli-kintone/tree/main/plugin-templates for available templates",
    type: "string",
    default: "javascript",
    requiresArg: true,
});
const handler = async (args) => {
    try {
        const flags = {
            name: args.name,
            template: args.template,
        };
        if (process.env.NODE_ENV === "test") {
            console.log(JSON.stringify({ flags: flags }));
        }
        else {
            await (0, init_1.run)(flags);
        }
    }
    catch (error) {
        log_1.logger.error(new error_1.RunError(error));
        // eslint-disable-next-line n/no-process-exit
        process.exit(1);
    }
};
exports.initCommand = {
    command,
    describe,
    builder,
    handler,
};
//# sourceMappingURL=init.js.map