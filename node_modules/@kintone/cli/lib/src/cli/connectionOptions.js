"use strict";
var _a;
Object.defineProperty(exports, "__esModule", { value: true });
exports.buildConnectionOptions = void 0;
const authOptions_1 = require("./authOptions");
const connectionOptions = {
    "base-url": {
        describe: "Kintone Base Url",
        default: process.env.KINTONE_BASE_URL,
        defaultDescription: "KINTONE_BASE_URL",
        type: "string",
        demandOption: true,
        requiresArg: true,
    },
    "basic-auth-username": {
        describe: "Kintone Basic Auth Username",
        default: process.env.KINTONE_BASIC_AUTH_USERNAME,
        defaultDescription: "KINTONE_BASIC_AUTH_USERNAME",
        type: "string",
        requiresArg: true,
    },
    "basic-auth-password": {
        describe: "Kintone Basic Auth Password",
        default: process.env.KINTONE_BASIC_AUTH_PASSWORD,
        defaultDescription: "KINTONE_BASIC_AUTH_PASSWORD",
        type: "string",
        requiresArg: true,
    },
    "pfx-file-path": {
        describe: "The path to client certificate file",
        type: "string",
        requiresArg: true,
    },
    "pfx-file-password": {
        describe: "The password of client certificate file",
        type: "string",
        requiresArg: true,
    },
    proxy: {
        describe: "The URL of a proxy server",
        default: (_a = process.env.HTTPS_PROXY) !== null && _a !== void 0 ? _a : process.env.https_proxy,
        defaultDescription: "HTTPS_PROXY",
        type: "string",
    },
};
const guestSpaceOptions = {
    "guest-space-id": {
        describe: "The ID of guest space",
        default: process.env.KINTONE_GUEST_SPACE_ID,
        defaultDescription: "KINTONE_GUEST_SPACE_ID",
        type: "string",
        requiresArg: true,
    },
};
const buildConnectionOptions = (args, config) => {
    // NOTE: Calling .options() in a loop breaks yargs type inference, so we use AuthResolvedArgv to supplement the auth argv types.
    let builder = args.options(connectionOptions);
    for (const [method, module] of Object.entries(authOptions_1.authModules)) {
        builder = builder.options(config.auth.includes(method)
            ? module.options
            : module.hiddenOptions);
    }
    const checked = builder
        .options(config.guestSpace
        ? guestSpaceOptions
        : {
            "guest-space-id": {
                ...guestSpaceOptions["guest-space-id"],
                hidden: true,
            },
        })
        .check((a) => (0, authOptions_1.checkAuth)(config.auth, a));
    if (config.auth.length > 1) {
        return checked.middleware((a) => (0, authOptions_1.resolveAuthPriority)(config.auth, a));
    }
    return checked;
};
exports.buildConnectionOptions = buildConnectionOptions;
//# sourceMappingURL=connectionOptions.js.map