"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createSchema = void 0;
const constants_1 = require("./constants");
const createSchema = (fieldsJson, transformer) => {
    const fields = transformer(convert(fieldsJson.properties));
    return { fields };
};
exports.createSchema = createSchema;
const convert = (properties) => {
    const fields = [];
    for (const property of Object.values(properties)) {
        if (!(0, constants_1.isSupportedField)(property)) {
            continue;
        }
        if (property.type === "SUBTABLE") {
            const { fields: fieldsInSubtable, ...others } = property;
            fields.push({
                fields: Object.values(fieldsInSubtable).filter(constants_1.isSupportedFieldInSubtable),
                ...others,
            });
            continue;
        }
        fields.push(property);
    }
    return fields;
};
//# sourceMappingURL=index.js.map