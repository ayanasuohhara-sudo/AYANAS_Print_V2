"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.run = void 0;
const client_1 = require("../../kintone/client");
const file_1 = require("../../utils/file");
const add_1 = require("./usecases/add");
const upsert_1 = require("./usecases/upsert");
const schema_1 = require("./schema");
const noop_1 = require("./schema/transformers/noop");
const userSelected_1 = require("./schema/transformers/userSelected");
const log_1 = require("../../utils/log");
const localRecordRepositoryFromStream_1 = require("./repositories/localRecordRepositoryFromStream");
const error_1 = require("../error");
const encoding_1 = require("../../utils/encoding");
const stability_1 = require("../../utils/stability");
const run = async (argv) => {
    try {
        const { app, filePath, encoding, attachmentsDir, updateKey, fields, useServerSideUpsert, ...restApiClientOptions } = argv;
        if (encoding) {
            await validateEncoding(filePath, encoding);
        }
        const apiClient = (0, client_1.buildRestAPIClient)(restApiClientOptions);
        const fieldsJson = await apiClient.app.getFormFields({ app });
        const schema = (0, schema_1.createSchema)(fieldsJson, fields
            ? (0, userSelected_1.userSelected)(fields, fieldsJson, updateKey)
            : (0, noop_1.noop)());
        const format = (0, file_1.extractFileFormat)(filePath);
        const localRecordRepository = new localRecordRepositoryFromStream_1.LocalRecordRepositoryFromStream(() => (0, file_1.openFsStreamWithEncode)(filePath, encoding), format, schema);
        if ((await localRecordRepository.length()) === 0) {
            log_1.logger.warn("The input file does not have any records");
            return;
        }
        const skipMissingFields = !fields;
        if (updateKey) {
            if (useServerSideUpsert) {
                (0, stability_1.emitDeprecationWarning)("Server-side upsert is now the default behavior. This flag is no longer needed and will be removed in a future version.", log_1.logger);
            }
            await (0, upsert_1.upsertRecords)(apiClient, app, localRecordRepository, schema, updateKey, {
                attachmentsDir,
                skipMissingFields,
            });
        }
        else {
            await (0, add_1.addRecords)(apiClient, app, localRecordRepository, schema, {
                attachmentsDir,
                skipMissingFields,
            });
        }
    }
    catch (e) {
        log_1.logger.error(new error_1.RunError(e));
        // eslint-disable-next-line n/no-process-exit
        process.exit(1);
    }
};
exports.run = run;
const validateEncoding = async (filePath, encoding) => {
    if (await (0, encoding_1.isMismatchEncoding)(filePath, encoding)) {
        throw new Error(`Failed to decode the specified CSV file.\nThe specified encoding (${encoding}) might mismatch the actual encoding of the CSV file.`);
    }
};
//# sourceMappingURL=index.js.map