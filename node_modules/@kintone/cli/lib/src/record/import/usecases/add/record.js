"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.recordConverter = void 0;
const recordConverter = async (record, schema, skipMissingFields, task) => {
    const newRecord = {};
    for (const fieldSchema of schema.fields) {
        if (!(fieldSchema.code in record.data)) {
            if (skipMissingFields) {
                continue;
            }
            else {
                throw new Error(`The specified field "${fieldSchema.code}" does not exist on the CSV`);
            }
        }
        newRecord[fieldSchema.code] = await task(record.data[fieldSchema.code], fieldSchema);
    }
    return newRecord;
};
exports.recordConverter = recordConverter;
//# sourceMappingURL=record.js.map