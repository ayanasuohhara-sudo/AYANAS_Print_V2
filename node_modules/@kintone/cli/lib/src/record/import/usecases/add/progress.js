"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ProgressLogger = void 0;
class ProgressLogger {
    constructor(logger, length, interval = 2000) {
        this.last = -1;
        this.logger = logger;
        this.length = length;
        this.interval = interval;
        this.next = this.interval;
    }
    update(succeededCount) {
        while (succeededCount >= this.next) {
            printProgress(this.logger, this.next, this.length);
            this.last = this.next;
            this.next += this.interval;
        }
    }
    abort(succeededCount) {
        this.logger.info(`Imported ${succeededCount} of ${this.length} records successfully`);
    }
    done() {
        this.logger.info(`Imported ${this.length} records successfully`);
    }
}
exports.ProgressLogger = ProgressLogger;
const printProgress = (logger, current, length) => {
    logger.info(`Imported ${current} of ${length} records`);
};
//# sourceMappingURL=progress.js.map