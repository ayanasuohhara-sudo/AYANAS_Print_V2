import type { Logger } from "../../../../utils/log";
export declare class ProgressLogger {
    private readonly logger;
    private readonly length;
    private readonly interval;
    private next;
    private last;
    constructor(logger: Logger, length: number, interval?: number);
    update(succeededCount: number): void;
    abort(succeededCount: number): void;
    done(): void;
}
