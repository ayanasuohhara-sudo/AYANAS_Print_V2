"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseKintoneRestAPIError = void 0;
/** *
 * @param error
 * @param chunkSize Chunk size of a single request. See https://github.com/kintone/js-sdk/blob/master/packages/rest-api-client/src/client/RecordClient.ts#L16
 * @param records Record of current chunk divided by rest-api-client
 * @param recordSchema
 */
const parseKintoneRestAPIError = (error, chunkSize, records, recordSchema) => {
    var _a, _b;
    let errorMessage = `${error.message}\n`;
    if (error.errors !== undefined) {
        const errors = error.errors;
        const orderedErrors = Object.entries(errors).sort((error1, error2) => {
            var _a, _b, _c, _d;
            const index1 = (_b = (_a = error1[0].match(/records\[(?<index>\d+)\]/)) === null || _a === void 0 ? void 0 : _a.groups) === null || _b === void 0 ? void 0 : _b.index;
            const index2 = (_d = (_c = error2[0].match(/records\[(?<index>\d+)\]/)) === null || _c === void 0 ? void 0 : _c.groups) === null || _d === void 0 ? void 0 : _d.index;
            if (index1 === undefined) {
                return 1;
            }
            if (index2 === undefined) {
                return -1;
            }
            return Number(index1) - Number(index2);
        });
        for (const [key, value] of orderedErrors) {
            const bulkRequestIndex = (_a = error.bulkRequestIndex) !== null && _a !== void 0 ? _a : 0;
            const indexMatch = key.match(/records\[(?<index>\d+)\]/);
            const recordIndex = Number((_b = indexMatch === null || indexMatch === void 0 ? void 0 : indexMatch.groups) === null || _b === void 0 ? void 0 : _b.index) + bulkRequestIndex * chunkSize;
            const fieldCode = getFieldCodeByErrorKeyWithSchema(key, recordSchema);
            const errorIndexMessage = generateErrorIndexMessage(records, recordIndex);
            errorMessage += `  An error occurred on ${fieldCode} ${errorIndexMessage}\n`;
            for (const message of value.messages) {
                errorMessage += `    Cause: ${message}\n`;
            }
        }
    }
    return errorMessage;
};
exports.parseKintoneRestAPIError = parseKintoneRestAPIError;
const generateErrorIndexMessage = (records, recordIndex) => {
    var _a;
    const formatInfo = (_a = records.at(recordIndex)) === null || _a === void 0 ? void 0 : _a.metadata.format;
    if (!formatInfo) {
        return "";
    }
    if (formatInfo.firstRowIndex === formatInfo.lastRowIndex) {
        return `at row ${formatInfo.lastRowIndex + 1}.`;
    }
    return `at rows from ${formatInfo.firstRowIndex + 1} to ${formatInfo.lastRowIndex + 1}.`;
};
const getFieldCodeByErrorKeyWithSchema = (errorKey, schema) => {
    /**
     * Parse error key to string array. For example:
     * Input: records[0].Table.value[1].value.table_text_0.value
     * Output: ['records', '0', 'Table', 'value', '1', 'value', 'table_text_0', 'value']
     */
    const parsedErrorKey = errorKey.split(/[[\].]+/);
    const fieldCodeIndex = 2;
    const fieldCode = parsedErrorKey[fieldCodeIndex];
    const field = schema.fields.find((f) => f.code === fieldCode);
    if (field === undefined) {
        return fieldCode;
    }
    switch (field.type) {
        case "SUBTABLE": {
            const fieldCodeIndexOfTableColumn = 6;
            return parsedErrorKey[fieldCodeIndexOfTableColumn];
        }
        default: {
            return fieldCode;
        }
    }
};
//# sourceMappingURL=error.js.map