"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.convertFieldValue = void 0;
const constants_1 = require("./constants");
const convertFieldValue = ({ type, value = "", }) => {
    switch (type) {
        case "SINGLE_LINE_TEXT":
        case "RADIO_BUTTON":
        case "MULTI_LINE_TEXT":
        case "NUMBER":
        case "RICH_TEXT":
        case "LINK":
        case "DROP_DOWN":
        case "DATETIME":
        case "TIME":
        case "UPDATED_TIME":
        case "CREATED_TIME":
            return { value };
        case "DATE":
            return { value: value.replace(/\//g, "-") };
        case "CREATOR":
        case "MODIFIER":
            return {
                value: {
                    code: value,
                },
            };
        case "MULTI_SELECT":
        case "CHECK_BOX":
            return { value: value ? value.split(constants_1.LINE_BREAK) : [] };
        case "USER_SELECT":
        case "ORGANIZATION_SELECT":
        case "GROUP_SELECT":
            return {
                value: value
                    ? value.split(constants_1.LINE_BREAK).map((code) => ({
                        code,
                    }))
                    : [],
            };
        case "FILE":
            return {
                value: value
                    ? value.split(constants_1.LINE_BREAK).map((localFilePath) => ({
                        localFilePath,
                    }))
                    : [],
            };
        default:
            return { value }; // TODO: Error handling
    }
};
exports.convertFieldValue = convertFieldValue;
//# sourceMappingURL=fieldValue.js.map