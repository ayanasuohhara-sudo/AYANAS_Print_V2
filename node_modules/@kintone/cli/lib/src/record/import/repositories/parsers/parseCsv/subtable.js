"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.convertSubtableField = void 0;
exports.subtableFieldReader = subtableFieldReader;
exports.fieldInSubtableReader = fieldInSubtableReader;
const fieldValue_1 = require("./fieldValue");
const convertSubtableField = (subtableField) => {
    const field = { value: [] };
    for (const subtableRow of subtableRowReader(subtableField)) {
        field.value.push(convertSubtableRow(subtableRow));
    }
    return field;
};
exports.convertSubtableField = convertSubtableField;
// eslint-disable-next-line func-style
function* subtableFieldReader(rows, schema) {
    const subtableFields = schema.fields.flatMap((field) => field.type === "SUBTABLE" ? [field] : []);
    for (const subtableField of subtableFields) {
        // pick rows which contains subtable related fields
        const subtableRows = rows.filter((row) => (subtableField.code in row && row[subtableField.code].length > 0) ||
            subtableField.fields.some(({ code }) => code in row && row[code].length > 0));
        if (subtableRows.length > 0) {
            yield {
                code: subtableField.code,
                rows: subtableRows,
                fields: subtableField.fields,
            };
        }
    }
}
const convertSubtableRow = (subtableRow) => {
    const newRow = {
        id: subtableRow.id,
        value: {},
    };
    for (const fieldsInSubtable of fieldInSubtableReader(subtableRow)) {
        newRow.value[fieldsInSubtable.code] =
            convertFieldInSubtable(fieldsInSubtable);
    }
    return newRow;
};
// eslint-disable-next-line func-style
function* subtableRowReader(subtableField) {
    for (const row of subtableField.rows) {
        yield { id: row[subtableField.code], row, fields: subtableField.fields };
    }
}
const convertFieldInSubtable = (fieldInSubtable) => {
    return (0, fieldValue_1.convertFieldValue)(fieldInSubtable);
};
// eslint-disable-next-line func-style
function* fieldInSubtableReader({ row, fields, }) {
    for (const fieldInSubtable of fields) {
        if (!(fieldInSubtable.code in row)) {
            continue;
        }
        yield {
            code: fieldInSubtable.code,
            value: row[fieldInSubtable.code],
            type: fieldInSubtable.type,
        };
    }
}
//# sourceMappingURL=subtable.js.map