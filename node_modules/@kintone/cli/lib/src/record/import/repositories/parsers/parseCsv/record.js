"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.convertRecord = void 0;
exports.recordReader = recordReader;
const field_1 = require("./field");
const subtable_1 = require("./subtable");
const constants_1 = require("./constants");
const stream_1 = require("stream");
const iterator_1 = require("../../../../../utils/iterator");
const convertRecord = (recordCsv, schema) => {
    const recordData = {};
    for (const field of (0, field_1.fieldReader)(recordCsv.rows[0], schema)) {
        recordData[field.code] = (0, field_1.convertField)(field);
    }
    for (const subtableField of (0, subtable_1.subtableFieldReader)(recordCsv.rows, schema)) {
        recordData[subtableField.code] = (0, subtable_1.convertSubtableField)(subtableField);
    }
    return {
        data: recordData,
        metadata: {
            format: {
                type: "csv",
                firstRowIndex: recordCsv.firstRowIndex,
                lastRowIndex: recordCsv.lastRowIndex,
            },
        },
    };
};
exports.convertRecord = convertRecord;
// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions/Arrow_functions#use_of_the_yield_keyword
// eslint-disable-next-line func-style
async function* recordReader(csvStream) {
    const lineOffset = 1; // offset the header row
    const { value: firstRow } = await csvStream[Symbol.asyncIterator]().next();
    if (firstRow === null || firstRow === undefined) {
        return;
    }
    const stream = unshiftToStream(csvStream, firstRow);
    const generator = (0, iterator_1.withIndex)((0, iterator_1.withNext)(stream[Symbol.asyncIterator]()));
    if (!hasSubtable(firstRow)) {
        for await (const { data: { current: row }, index: rowIndex, } of generator) {
            yield {
                rows: [row],
                firstRowIndex: rowIndex + lineOffset,
                lastRowIndex: rowIndex + lineOffset,
            };
        }
        return;
    }
    let rows = [];
    let firstRowIndex = 0;
    for await (const { data: { current: currentRow, next: nextRow }, index: rowIndex, } of generator) {
        rows.push(currentRow);
        if (nextRow === undefined || isPrimaryCsvRow(nextRow)) {
            if (rows.length > 0 && isPrimaryCsvRow(rows[0])) {
                yield {
                    rows: rows,
                    firstRowIndex: firstRowIndex + lineOffset,
                    lastRowIndex: rowIndex + lineOffset,
                };
            }
            firstRowIndex = rowIndex + 1;
            rows = [];
        }
    }
}
const hasSubtable = (row) => constants_1.PRIMARY_MARK in row;
const isPrimaryCsvRow = (row) => !!row[constants_1.PRIMARY_MARK];
const unshiftToStream = (stream, element) => stream_1.Readable.from((async function* () {
    yield element;
    yield* stream;
})());
//# sourceMappingURL=record.js.map