import type { RecordNumber } from "../types/field";
export declare const parseRecords: (options: {
    source: string;
    format: string;
    recordNumberFieldCode: string;
}) => Promise<RecordNumber[]>;
