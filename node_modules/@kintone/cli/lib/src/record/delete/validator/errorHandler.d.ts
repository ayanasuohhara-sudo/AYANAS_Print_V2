import type { RecordNumber } from "../types/field";
import { ValidatorError } from "./error";
export declare class ErrorHandler {
    private readonly invalidValue;
    private readonly notExists;
    private readonly duplicated;
    constructor();
    addInvalidValueError: (recordNumber: RecordNumber) => number;
    addNotExistsError: (recordNumber: RecordNumber) => number;
    addDuplicatedError: (recordNumber: RecordNumber) => number;
    hasError: () => boolean;
    generateError: () => ValidatorError;
    generateReportedId: (recordNumbers: RecordNumber[]) => string;
}
