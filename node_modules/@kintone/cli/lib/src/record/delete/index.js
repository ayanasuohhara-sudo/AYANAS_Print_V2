"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.run = void 0;
const client_1 = require("../../kintone/client");
const deleteAll_1 = require("./usecases/deleteAll");
const deleteByRecordNumber_1 = require("./usecases/deleteByRecordNumber");
const log_1 = require("../../utils/log");
const file_1 = require("../../utils/file");
const encoding_1 = require("../../utils/encoding");
const parsers_1 = require("./parsers");
const error_1 = require("../error");
const run = async (options) => {
    try {
        const { app, filePath, encoding, ...restApiClientOptions } = options;
        const apiClient = (0, client_1.buildRestAPIClient)(restApiClientOptions);
        if (filePath) {
            await deleteRecordsByFile(apiClient, app, filePath, encoding);
            return;
        }
        await (0, deleteAll_1.deleteAllRecords)(apiClient, app);
    }
    catch (e) {
        log_1.logger.error(new error_1.RunError(e));
        // eslint-disable-next-line n/no-process-exit
        process.exit(1);
    }
};
exports.run = run;
const deleteRecordsByFile = async (apiClient, app, filePath, encoding) => {
    if (encoding) {
        await validateEncoding(filePath, encoding);
    }
    const recordNumbers = await getRecordNumbersFromFile(apiClient, app, filePath, encoding);
    if (recordNumbers.length === 0) {
        log_1.logger.warn("The specified CSV file does not have any records.");
        return;
    }
    await (0, deleteByRecordNumber_1.deleteByRecordNumber)(apiClient, app, recordNumbers);
};
const getRecordNumbersFromFile = async (apiClient, app, filePath, encoding) => {
    const fieldsJson = await apiClient.app.getFormFields({ app });
    const recordNumberFieldCode = getRecordNumberFieldCode(fieldsJson.properties);
    const { content, format } = await (0, file_1.readFile)(filePath, encoding);
    return (0, parsers_1.parseRecords)({
        source: content,
        format,
        recordNumberFieldCode,
    });
};
const getRecordNumberFieldCode = (properties) => {
    let recordNumberFieldCode = "";
    for (const property of Object.values(properties)) {
        if (property.type === "RECORD_NUMBER") {
            recordNumberFieldCode = property.code;
            break;
        }
    }
    return recordNumberFieldCode;
};
const validateEncoding = async (filePath, encoding) => {
    if (await (0, encoding_1.isMismatchEncoding)(filePath, encoding)) {
        throw new Error(`Failed to decode the specified CSV file.\nThe specified encoding (${encoding}) might mismatch the actual encoding of the CSV file.`);
    }
};
//# sourceMappingURL=index.js.map