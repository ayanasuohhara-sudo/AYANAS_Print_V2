"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.tableLayoutComparator = void 0;
const tableLayoutComparator = (fieldCode, layoutJson) => {
    const tableField = layoutJson.layout.find((row) => row.type === "SUBTABLE" && row.code === fieldCode);
    return (fieldA, fieldB) => {
        if (!tableField) {
            return 0;
        }
        const indexA = tableField.fields.findIndex((field) => field.code === fieldA.code);
        const indexB = tableField.fields.findIndex((field) => field.code === fieldB.code);
        if (indexA === -1) {
            return 1;
        }
        if (indexB === -1) {
            return -1;
        }
        return indexA - indexB;
    };
};
exports.tableLayoutComparator = tableLayoutComparator;
//# sourceMappingURL=table.js.map