import type { LocalRecord } from "../../../types/record";
import type * as Fields from "../../../types/field";
import type { RecordSchema } from "../../../types/schema";
type Field = {
    code: string;
    value: Fields.OneOf;
};
export declare const convertField: (field: Field, useLocalFilePath: boolean) => string;
export declare function fieldReader(record: LocalRecord, schema: RecordSchema): Generator<Field, void, undefined>;
export {};
