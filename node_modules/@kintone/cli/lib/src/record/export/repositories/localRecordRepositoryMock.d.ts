import type { LocalRecordRepository, Writer } from "../usecases/interface";
import type { LocalRecord } from "../types/record";
export declare class LocalRecordRepositoryMock implements LocalRecordRepository {
    readonly format = "csv";
    private underlyingSink;
    writer(): WritableMock;
    receivedRecords(): LocalRecord[];
}
declare class WritableMock implements Writer {
    underlyingSink: LocalRecord[];
    write(chunk: LocalRecord[]): Promise<void>;
    end(): Promise<void>;
}
export {};
