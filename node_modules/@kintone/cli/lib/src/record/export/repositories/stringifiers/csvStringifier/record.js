"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.convertRecord = void 0;
exports.recordReader = recordReader;
const field_1 = require("./field");
const subtable_1 = require("./subtable");
const constants_1 = require("./constants");
const convertRecord = (record, schema, useLocalFilePath) => {
    const primaryRow = {};
    for (const field of (0, field_1.fieldReader)(record, schema)) {
        primaryRow[field.code] = (0, field_1.convertField)(field, useLocalFilePath);
    }
    if (!schema.hasSubtable) {
        return [primaryRow];
    }
    const subtableRows = [];
    for (const subtableField of (0, subtable_1.subtableFieldReader)(record, schema)) {
        subtableRows.push(...(0, subtable_1.convertSubtableField)(subtableField, useLocalFilePath));
    }
    if (subtableRows.length === 0) {
        primaryRow[constants_1.PRIMARY_MARK] = constants_1.PRIMARY_MARK;
        return [primaryRow];
    }
    // merge primaryRow fields to all subtable rows
    const recordCsvRows = subtableRows.map((subtableRow) => ({
        ...primaryRow,
        ...subtableRow,
    }));
    recordCsvRows[0][constants_1.PRIMARY_MARK] = constants_1.PRIMARY_MARK;
    return recordCsvRows;
};
exports.convertRecord = convertRecord;
// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions/Arrow_functions#use_of_the_yield_keyword
// eslint-disable-next-line func-style
function* recordReader(records) {
    yield* records;
}
//# sourceMappingURL=record.js.map