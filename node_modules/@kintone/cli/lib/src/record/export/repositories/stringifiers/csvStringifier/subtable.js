"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.convertSubtableField = exports.hasSubtable = void 0;
exports.subtableFieldReader = subtableFieldReader;
const fieldValue_1 = require("./fieldValue");
const hasSubtable = (fieldsJson) => Object.values(fieldsJson.properties).some((field) => field.type === "SUBTABLE");
exports.hasSubtable = hasSubtable;
const convertSubtableField = (field, useLocalFilePath) => {
    const subtableRows = [];
    for (const row of subtableRowReader(field)) {
        subtableRows.push(convertSubtableRow(row, field.code, field.fields, useLocalFilePath));
    }
    return subtableRows;
};
exports.convertSubtableField = convertSubtableField;
// eslint-disable-next-line func-style
function* subtableFieldReader(record, schema) {
    for (const field of schema.fields) {
        if (field.type !== "SUBTABLE") {
            continue;
        }
        if (!(field.code in record)) {
            throw new Error(`The record is missing a field (${field.code})`);
        }
        yield {
            code: field.code,
            value: record[field.code],
            fields: field.fields,
        };
    }
}
const convertSubtableRow = (subtableRow, subtableFieldCode, subtableFields, useLocalFilePath) => {
    const newRow = {
        [subtableFieldCode]: subtableRow.id,
    };
    for (const fieldsInSubtable of fieldsInSubtableReader(subtableRow, subtableFieldCode, subtableFields)) {
        newRow[fieldsInSubtable.code] = convertFieldInSubtable(fieldsInSubtable, useLocalFilePath);
    }
    return newRow;
};
// eslint-disable-next-line func-style
function* subtableRowReader(subtableField) {
    yield* subtableField.value.value;
}
const convertFieldInSubtable = (fieldInSubtable, useLocalFilePath) => {
    return (0, fieldValue_1.convertFieldValue)(fieldInSubtable.value, useLocalFilePath);
};
// eslint-disable-next-line func-style
function* fieldsInSubtableReader(subtableRow, subtableFieldCode, subtableFields) {
    for (const fieldPropertyInSubtable of subtableFields) {
        if (!(fieldPropertyInSubtable.code in subtableRow.value)) {
            continue;
        }
        yield {
            code: fieldPropertyInSubtable.code,
            value: subtableRow.value[fieldPropertyInSubtable.code],
        };
    }
}
//# sourceMappingURL=subtable.js.map