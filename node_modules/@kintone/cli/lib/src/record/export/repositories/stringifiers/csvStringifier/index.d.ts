import type { LocalRecord } from "../../../types/record";
import type { RecordSchema } from "../../../types/schema";
import type { Stringifier } from "../index";
export declare class CsvStringifier implements Stringifier {
    private readonly recordTransformer;
    private readonly csvStringifier;
    private readonly encoder;
    constructor(schema: RecordSchema, useLocalFilePath: boolean);
    write(records: LocalRecord[]): Promise<void>;
    read(size?: number): any;
    end(): Promise<void>;
    pipe<T extends NodeJS.WritableStream>(destination: T): void;
    [Symbol.asyncIterator](): AsyncGenerator<string, void, undefined>;
}
