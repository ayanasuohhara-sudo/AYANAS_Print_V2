import type { LocalRecordRepository } from "../usecases/interface";
import type { RecordSchema } from "../types/schema";
export declare class LocalRecordRepositoryFromStream implements LocalRecordRepository {
    readonly format = "csv";
    private readonly openWritableSink;
    private readonly schema;
    private readonly useLocalFilePath;
    constructor(openWritableSink: () => NodeJS.WritableStream, schema: RecordSchema, useLocalFilePath: boolean);
    writer(): import("./stringifiers").Stringifier;
}
