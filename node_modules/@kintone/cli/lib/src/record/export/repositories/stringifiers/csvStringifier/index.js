"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.CsvStringifier = void 0;
const csv_stringify_1 = require("csv-stringify");
const record_1 = require("./record");
const constants_1 = require("./constants");
const header_1 = require("./header");
const stream_1 = require("stream");
class CsvStringifier {
    constructor(schema, useLocalFilePath) {
        const recordTransform = new RecordsTransform(schema, useLocalFilePath);
        const headerFields = (0, header_1.buildHeaderFields)(schema);
        const csvStringifier = (0, csv_stringify_1.stringify)({
            columns: headerFields,
            header: true,
            delimiter: constants_1.SEPARATOR,
            record_delimiter: constants_1.LINE_BREAK,
            quoted_match: /^(?!\*$).+$/,
            quoted_empty: false,
        });
        this.recordTransformer = recordTransform;
        this.csvStringifier = csvStringifier;
        this.encoder = new stream_1.PassThrough({ encoding: "utf-8" });
        this.recordTransformer.pipe(this.csvStringifier).pipe(this.encoder);
    }
    async write(records) {
        return new Promise((resolve, reject) => {
            this.recordTransformer.write(records, (e) => {
                if (e) {
                    reject(e);
                }
                resolve();
            });
        });
    }
    read(size) {
        return this.encoder.read(size);
    }
    async end() {
        return new Promise((resolve) => {
            this.recordTransformer.end(resolve);
        });
    }
    pipe(destination) {
        this.encoder.pipe(destination);
    }
    async *[Symbol.asyncIterator]() {
        yield* this.encoder;
    }
}
exports.CsvStringifier = CsvStringifier;
class RecordsTransform extends stream_1.Transform {
    constructor(schema, useLocalFilePath) {
        super({ objectMode: true });
        this.schema = schema;
        this.useLocalFilePath = useLocalFilePath;
    }
    _transform(chunk, encoding, done) {
        const csvRows = [];
        for (const record of (0, record_1.recordReader)(chunk)) {
            csvRows.push(...(0, record_1.convertRecord)(record, this.schema, this.useLocalFilePath));
        }
        for (const csvRow of csvRows) {
            this.push(csvRow);
        }
        done();
    }
}
//# sourceMappingURL=index.js.map