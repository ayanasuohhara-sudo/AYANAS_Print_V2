(() => {
    'use strict';

    const EstimateTemplate = TemplateInterface.create('EstimateTemplate', () => {
        throw new Error('見積書テンプレートは未実装です。');
    });

    window.EstimateTemplate = EstimateTemplate;

})();
