(() => {
    'use strict';

    const LabelTemplate = TemplateInterface.create('LabelTemplate', () => {
        throw new Error('ラベルテンプレートは未実装です。');
    });

    window.LabelTemplate = LabelTemplate;

})();
